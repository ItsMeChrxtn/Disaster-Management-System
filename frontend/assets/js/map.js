const caviteBounds=L.latLngBounds(Config.MAP_BOUNDS);
const map=L.map('map',{zoomControl:true,maxBounds:caviteBounds,maxBoundsViscosity:1,minZoom:9}).setView(Config.MAP_CENTER,Config.MAP_ZOOM);
const streetLayer=L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);
const satelliteLayer=L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,attribution:'Tiles &copy; Esri'});
L.control.layers({'Street map':streetLayer,'Satellite imagery':satelliteLayer},null,{position:'bottomright'}).addTo(map);
const riskColors={low:'#10b981',moderate:'#f59e0b',high:'#f97316',critical:'#dc2626'},layerColors={flood:'#2563eb',storm:'#7c3aed',earthquake:'#f97316'};
const layers={flood:L.featureGroup().addTo(map),storm:L.featureGroup().addTo(map),earthquake:L.featureGroup().addTo(map),safeZones:L.featureGroup().addTo(map),centers:L.featureGroup().addTo(map),routes:L.featureGroup().addTo(map)};
let searchMarker,municipalitiesLoaded=false;
function safe(value){const d=document.createElement('div');d.textContent=value??'';return d.innerHTML;}
function hazardPopup(item,label){return `<div class="min-w-48"><strong>${safe(item.hazard_name)}</strong><p>${safe(label)} · <b>${safe(item.risk_level)} risk</b></p><p>${safe(item.description||'No description')}</p><small>${safe(item.municipality_name||'Province-wide')}</small></div>`;}
function addHazards(rows,layer,key,label){rows.forEach(item=>L.geoJSON(item.geojson_data,{style:{color:layerColors[key],weight:3,fillColor:riskColors[item.risk_level]||layerColors[key],fillOpacity:.28},pointToLayer:(feature,latlng)=>L.circleMarker(latlng,{radius:9,color:layerColors[key],fillColor:riskColors[item.risk_level]||layerColors[key],fillOpacity:.85})}).bindPopup(hazardPopup(item,label)).addTo(layer));}
function markerIcon(color,symbol){return L.divIcon({className:'',html:`<span style="display:grid;place-items:center;width:30px;height:30px;border:3px solid white;border-radius:50%;background:${color};color:white;font-weight:800;box-shadow:0 2px 8px #0005">${symbol}</span>`,iconSize:[30,30],iconAnchor:[15,15],popupAnchor:[0,-14]});}
function addSafeZones(rows){rows.forEach(item=>L.marker([item.latitude,item.longitude],{icon:markerIcon('#10b981','S')}).bindPopup(`<strong>${safe(item.safezone_name)}</strong><p>${safe(item.address)}</p><p>Capacity: <b>${item.capacity}</b></p><small>${safe(item.municipality_name)}</small>`).addTo(layers.safeZones));}
function addCenters(rows){rows.forEach(item=>L.marker([item.latitude,item.longitude],{icon:markerIcon('#3730a3','E')}).bindPopup(`<strong>${safe(item.center_name)}</strong><p>${safe(item.address)}</p><p>Status: <b>${safe(item.status)}</b> · Capacity: <b>${item.capacity}</b></p>${item.contact_number?`<p>Contact: ${safe(item.contact_number)}</p>`:''}<small>${safe(item.municipality_name)}</small>`).addTo(layers.centers));}
function addRoutes(rows){rows.forEach(item=>L.geoJSON(item.geojson_data,{style:{color:'#0f766e',weight:5,opacity:.8}}).bindPopup(`<strong>${safe(item.route_name)}</strong><p>${safe(item.start_location?.name)} → ${safe(item.end_location?.name)}</p><small>${safe(item.municipality_name||'Province-wide')}</small>`).addTo(layers.routes));}
function visibleBounds(){const bounds=L.latLngBounds([]);Object.values(layers).forEach(layer=>{if(map.hasLayer(layer)){const candidate=layer.getBounds();if(candidate.isValid())bounds.extend(candidate);}});return bounds;}
function constrainedBounds(bounds){if(!bounds.isValid())return null;const south=Math.max(bounds.getSouth(),caviteBounds.getSouth()),west=Math.max(bounds.getWest(),caviteBounds.getWest()),north=Math.min(bounds.getNorth(),caviteBounds.getNorth()),east=Math.min(bounds.getEast(),caviteBounds.getEast());return south<=north&&west<=east?L.latLngBounds([south,west],[north,east]):null;}
function fitVisible(){const bounds=constrainedBounds(visibleBounds());if(bounds)map.fitBounds(bounds,{padding:[35,35],maxZoom:15});else toast('No visible Cavite map features to fit');}
async function loadGisLayers(fit=false){const status=document.getElementById('layerStatus');status.textContent='Loading GIS data…';try{const mid=document.getElementById('municipalityFilter').value,query=mid?`?municipality_id=${encodeURIComponent(mid)}`:'',data=await Api.get(`/gis/layers${query}`);Object.values(layers).forEach(layer=>layer.clearLayers());addHazards(data.flood_layer,layers.flood,'flood','Flood zone');addHazards(data.storm_surge_layer,layers.storm,'storm','Storm surge area');addHazards(data.earthquake_layer,layers.earthquake,'earthquake','Earthquake area');addSafeZones(data.safe_zone_layer);addCenters(data.evacuation_center_layer);addRoutes(data.route_layer);if(!municipalitiesLoaded){const select=document.getElementById('municipalityFilter');data.municipalities.forEach(item=>select.add(new Option(item.municipality_name,item.id)));municipalitiesLoaded=true;}const total=data.flood_layer.length+data.storm_surge_layer.length+data.earthquake_layer.length+data.safe_zone_layer.length+data.evacuation_center_layer.length+data.route_layer.length;status.textContent=`${total} mapped feature${total===1?'':'s'} in Cavite`;if(fit)fitVisible();}catch(error){status.textContent='GIS data unavailable';toast(error.message);}}
document.getElementById('layerToggles').addEventListener('change',event=>{const input=event.target.closest('input[data-layer]');if(!input)return;const layer=layers[input.dataset.layer];input.checked?layer.addTo(map):map.removeLayer(layer);});
document.getElementById('municipalityFilter').addEventListener('change',()=>loadGisLayers(true));
document.getElementById('hazardTypeFilter')?.addEventListener('change',event=>{const selected=event.target.value;['flood','storm','earthquake'].forEach(key=>{const checkbox=document.querySelector(`[data-layer="${key}"]`),visible=!selected||selected===key;checkbox.checked=visible;visible?layers[key].addTo(map):map.removeLayer(layers[key]);});fitVisible();});
document.getElementById('fitLayersBtn').addEventListener('click',fitVisible);
document.getElementById('fullscreenMapBtn')?.addEventListener('click',()=>{const shell=document.getElementById('publicMapShell');if(!document.fullscreenElement)shell.requestFullscreen?.();else document.exitFullscreen?.();});
document.addEventListener('fullscreenchange',()=>{setTimeout(()=>map.invalidateSize(),120);const button=document.getElementById('fullscreenMapBtn');if(button)button.textContent=document.fullscreenElement?'Exit full screen':'Full screen';});
document.getElementById('locationSearchForm').addEventListener('submit',async event=>{event.preventDefault();const input=document.getElementById('locationSearch'),button=event.currentTarget.querySelector('button'),query=input.value.trim();if(query.length<2)return;button.disabled=true;button.textContent='Searching…';try{const viewbox='120.4161392,14.5856400,121.0776824,14.0568738',search=`${query}, Cavite, Philippines`,response=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=5&countrycodes=ph&bounded=1&viewbox=${viewbox}&q=${encodeURIComponent(search)}`,{headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Location search is temporarily unavailable');const results=await response.json();if(!results.length)throw new Error('Location not found within Cavite');const result=results[0],lat=Number(result.lat),lng=Number(result.lon);if(!caviteBounds.contains([lat,lng]))throw new Error('Location is outside Cavite');if(searchMarker)searchMarker.remove();searchMarker=L.marker([lat,lng]).addTo(map).bindPopup(`<strong>Cavite search result</strong><p>${safe(result.display_name)}</p>`).openPopup();const resultBounds=result.boundingbox?.length===4?constrainedBounds(L.latLngBounds([[Number(result.boundingbox[0]),Number(result.boundingbox[2])],[Number(result.boundingbox[1]),Number(result.boundingbox[3])]])):null;if(resultBounds)map.fitBounds(resultBounds,{maxZoom:17});else map.setView([lat,lng],16);}catch(error){toast(error.message);}finally{button.disabled=false;button.textContent='Search';}});
document.getElementById('locateBtn').addEventListener('click',()=>map.locate({setView:false,maxZoom:16,enableHighAccuracy:true}));
map.on('locationfound',event=>{if(!caviteBounds.contains(event.latlng)){toast('Your current location is outside Cavite');map.fitBounds(caviteBounds);return;}if(searchMarker)searchMarker.remove();searchMarker=L.circleMarker(event.latlng,{radius:9,color:'#1d4ed8',fillColor:'#60a5fa',fillOpacity:1}).addTo(map).bindPopup(`Your approximate location<br><small>Accuracy: ${Math.round(event.accuracy)} m</small>`).openPopup();map.setView(event.latlng,16);});
map.on('locationerror',()=>toast('Unable to access your location'));
if(Api.token()){document.getElementById('accountLink').textContent='Dashboard';document.getElementById('accountLink').href='dashboard.html';}
loadGisLayers();
async function loadAlerts(){try{const alerts=await Api.get('/alerts'),el=document.getElementById('alertStrip');if(!alerts.length){el.classList.add('hidden');return;}const alert=alerts[0];el.innerHTML=`<strong>${safe(alert.title)}</strong><p class="text-sm">${safe(alert.message)}</p>`;el.classList.remove('hidden');}catch{}}
loadAlerts();setInterval(loadAlerts,Config.ALERT_POLL_MS);
